
function executeBatch(data) {
    console.log("Calling Judge0 with data", data);
    data = []
    //  call Judge0 and get n tokens

    //  create n executions {subId, token}
  }

  function excuteSingle() {
    //   for run
  }

  module.exports = {executeBatch};
